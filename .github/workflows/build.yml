name: Build Workflow

on:
  pull_request:
  push:

jobs:
  build-package:
    uses: usdot-fhwa-stol/actions/.github/workflows/cpp-module-build.yml@ddfb9b021c0ff09ec38fd7857ded9e34ee2efe0d
    with:
      build-image-tag: debian_version_match_so
    secrets:
      aws-access-key-id: ${{ secrets.APT_REPO_AWS_ACCESS_KEY_ID }}
      aws-secret-access-key: ${{ secrets.APT_REPO_AWS_SECRET_ACCESS_KEY }}
